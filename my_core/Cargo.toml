# this crate is dependencies-agnostic and platform--agnostic
# dont add any external dependencies other than serde
[package]
name = "my_core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true }
agnostic-lite = { workspace = true, features = ["time"] }

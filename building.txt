clear &&
cd ~/Documents/backup_folder_for_hashem/accounting_app &&
cargo clean &&
RUSTFLAGS="-Awarnings" cargo build -p accounting_lib &&

RUSTFLAGS="-Awarnings" dx serve -p accounting_ui &&
du -sh ./target &&
zed . &&
exit

clear && cockroach start-single-node --insecure
clear && cockroach sql --insecure
clear && cockroach sql --file db_client_cockroach/schema/tables.sql --insecure
clear && RUSTFLAGS="-A warnings" cargo test -p db_client_cockroach tests::test_insert_user -- --show-output
clear && dx serve -p accounting_ui

git:
clear &&
git add . &&
git commit -m "" &&
git push origin main

clear && RUSTFLAGS="-Awarnings" dx serve -p accounting_ui
clear && RUSTFLAGS="-Awarnings" cargo run -p server_actix

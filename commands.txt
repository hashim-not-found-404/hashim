git ls-files "*.rs" | xargs wc -l | tail -1
git checkout -b new_branch_name
du -sh ./target

code-ranker report .
xdg-open path_to_html_file
dep_graph_rs ./my_core | dot -Tpng > graph.png

clear && cargo-cycles --dot graph.dot
dot -Tpng graph.dot -o graph.png
xdot graph.dot
splines=ortho;

cargo-cycles --topo-sort

cargo sort -w

clear && cockroach start-single-node --insecure
clear && cockroach sql --insecure
clear && cockroach sql --file db_client_cockroach/schema/tables.sql --insecure

clear && RUSTFLAGS="-A warnings" dx fmt -p accounting_ui
clear && RUSTFLAGS="-A warnings" dx serve -p accounting_ui --hot-reload=false --port 8082
clear && RUSTFLAGS="-A warnings" cargo check
clear && RUSTFLAGS="-A warnings" cargo check -p my_core
clear && RUSTFLAGS="-A warnings" cargo run -p server_actix
clear && RUSTFLAGS="-A warnings" cargo +nightly udeps --all-targets
clear && cargo upgrade --verbose
